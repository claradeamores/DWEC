console.log(isNaN("Hola" * 5));
//no es un número por lo que nos devuelve true porque no es un número
console.log(isNaN("3" * 5));
//nos devuelve false porque hace la operacion y el resultado es un número
